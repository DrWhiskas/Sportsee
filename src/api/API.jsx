import React, { useState, useEffect } from "react";
import axios from 'axios'


export default function API(){

/*    axios.get('.../backend/app/data.js')
    .then((res) =>{
        setData(res.data)
    })
    .catch(error =>{
        console.error('Erreur', error);
    })
    if(!data) return null*/
    return(
        <>
        </>
    )
}
